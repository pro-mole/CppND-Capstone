// The roguelite map

#include "component.hxx"
#include "entity.hxx"

namespace Rogue {

    ECS::Entity& createRogueMap() {
        // Creates the player entity and its basic components
    }

    ECS::Compponent& readMapFromFile(std::string filePath) {
        // Reads map data and translate it into the map grid component
    }

}  // namespace Rogue